name: Release FBP Flow Tool UI
on:
  push:
    branches:
      - main
permissions:
  contents: write
  pull-requests: write
jobs:
  release-please:
    uses: dakispro/.github/.github/workflows/release.yml@main
    secrets: inherit
